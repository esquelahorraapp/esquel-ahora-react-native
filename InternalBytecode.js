// InternalBytecode.js
// Placeholder file to avoid ENOENT errors from Metro when a stack trace
// references "InternalBytecode.js" (Hermes/bytecode addresses).
// This is intentionally minimal — Metro only needs the file to exist
// so it can read a code frame. Safe to remove if a better workaround is found.

module.exports = {};
